[build]
builder = "NIXPACKS"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Railway 环境提示：
# - 请在 Variables 里设置：DEEPSEEK_API_KEY、INTRADAY_ASSISTANT、SELECTION_MODE、UNIVERSE 等
# - 时区：默认在程序内固定为 Asia/Shanghai；无需 Railway 注入 TZ
# - Procfile 已声明 web（看板）与 worker（调度）两个进程
